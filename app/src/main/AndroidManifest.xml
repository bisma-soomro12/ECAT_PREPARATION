<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ecat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".score"></activity>
        <activity android:name=".logout" />
        <activity android:name=".important_points" />
        <activity android:name=".keep_notes1" />
        <activity android:name=".add_notes" />
        <activity android:name=".add_notes1" />
        <activity android:name=".KeepNotes" />
        <activity android:name=".home" />
        <activity android:name=".sign_in" />
        <activity android:name=".section" />
        <activity android:name=".question" />
        <activity android:name=".signup" />
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>